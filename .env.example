# MongoDB Configuration
# Root admin credentials (for MongoDB admin tasks)
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=adminpassword

# Application database credentials (configured in mongodb-init/init-mongo.js)
MONGO_APP_USERNAME=deeka
MONGO_APP_PASSWORD=wwmib3112
MONGO_INITDB_DATABASE=sudden

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# Backend Configuration
SERVER_PORT=8081

# Frontend Configuration
FRONTEND_PORT=80
